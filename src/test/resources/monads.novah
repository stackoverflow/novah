module monads

bind : Option a -> (a -> Option b) -> Option b
bind opt f =
  case opt of
    Some x -> f x
    None -> None

optionBuilder =
  { bind: bind
  , return: Some
  }

foo = option.do
  let! x = Some 3
  let! y = Some 9
  return (x + y)